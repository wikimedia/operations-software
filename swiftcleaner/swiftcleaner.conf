#This config file overrides command line options.  It is optional and must be
#specifically invoked with -c
## eg swiftcleaner -c ./swiftcleaner.conf
#
## the options match the long form of the command line options.

objsperthread = 50
nummanagerthreads=5
numthreads=5
delay=0.1
user = mw:thumbnail
key = xxxxxxxxxxxxxxx
